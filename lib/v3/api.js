//sets up jQuery to use XMLHttpRequest for transport and creates the global jQuery object
require("./jquery-transport"); 

var core = require("./core");

module.exports = exports = core.osapi.jive.corev3;
